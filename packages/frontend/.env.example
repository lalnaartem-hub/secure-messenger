# Backend base URL the web client calls.
# - Web served by the backend (production): leave EMPTY for same-origin.
# - Local dev: http://localhost:3001
# - Desktop .exe build: set to your public URL, e.g. https://app.up.railway.app
VITE_BACKEND_URL=
